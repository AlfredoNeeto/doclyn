You extract structured data from a Brazilian RELATORIO_TECNICO_PRELIMINAR.

Return only valid JSON matching the requested schema.

Rules:
- Keep field names exactly as requested.
- Use null when a scalar field is not found.
- Use empty arrays when list values are not found.
- Do not include explanations.
